# Port for the server (Render will set this automatically)
PORT=3001

# Frontend URL for CORS (optional)
# Leave empty for single-service deployment
# Set for separate deployments or local development
CLIENT_URL=
